"""Local-first RAG retrieval evaluation lab.

The core of this package intentionally depends only on the Python standard
library so it runs fully offline, with no API keys and no data leaving the host.
"""

__version__ = "0.1.0"
